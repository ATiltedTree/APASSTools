#pragma once

constexpr const char *CONFIG_APP_VERSION = "${PROJECT_VERSION}";
constexpr const char *CONFIG_APP_NAME = "${CMAKE_PROJECT_NAME}";
constexpr const char *CONFIG_APP_URL = "${CMAKE_PROJECT_HOMEPAGE_URL}";
constexpr const char *CONFIG_COMPILER = "${CMAKE_CXX_COMPILER_ID}";
constexpr const char *CONFIG_COMPILER_VERSION = "${CMAKE_CXX_COMPILER_VERSION}";
constexpr const char *CONFIG_COMPILER_ARCH = "${CMAKE_SYSTEM_PROCESSOR}";
