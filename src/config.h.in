#pragma once

#include <filesystem>

#define CONFIG_APP_VERSION "@PROJECT_VERSION@"
#define CONFIG_APP_NAME "@CMAKE_PROJECT_NAME@"
#define CONFIG_APP_URL "@CMAKE_PROJECT_HOMEPAGE_URL@"
#define CONFIG_COMPILER "@CMAKE_CXX_COMPILER_ID@"
#define CONFIG_COMPILER_VERSION "@CMAKE_CXX_COMPILER_VERSION@"
#define CONFIG_COMPILER_ARCH "@CMAKE_SYSTEM_PROCESSOR@"
#define CONFIG_LOCAL_DEV "@LOCAL_DEV@"
#if defined(CONFIG_LOCAL_DEV)
static std::filesystem::path CONFIG_ICON_PATH =
    std::filesystem::current_path().append("../data/resources/apasstools.svg").lexically_normal();
#else
static std::filesystem::path CONFIG_ICON_PATH =
    std::filesystem::current_path()
        .append("../share/apasstools/resources/apasstools.svg")
        .lexically_normal();
#endif
